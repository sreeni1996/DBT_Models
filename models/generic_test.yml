models:
  - name: employee_raw
    columns:
      - name: salary
        tests:
          - not_null
      - name: emp_id
        tests:
          - not_null
          - unique
      - name: dept_id
        tests:
          - accepted_values:
              arguments:
                values: [10, 20, 30]


