sources:
  - name: finance_base
    database: PROD
    schema: PAYROLL_RAW
    tables:
      - name: employee
        columns:
          - name: emp_id
            tests:
              - unique
              - not_null
      - name: department
        columns:
          - name: dept_id
            tests:
              - not_null
              

